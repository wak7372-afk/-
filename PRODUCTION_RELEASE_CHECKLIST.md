# قائمة إطلاق مركز ذات خيل لتعليم القرآن الكريم وعلومه

هذه القائمة تفصل بين ما تم تنفيذه داخل المستودع وما يجب تنفيذه على مشروع Supabase والاستضافة قبل استخدام بيانات حقيقية.

## جاهز داخل المستودع

- دخول محلي باسم المستخدم وكلمة المرور عبر Edge Function، مع تعطيل مسار المعاينة التلقائي.
- إنشاء الحسابات من لوحة المدير، مع إجبار الحساب الجديد على تغيير كلمة المرور.
- ربط Google من إعدادات الحساب، واستعادة كلمة المرور عبر OTP إلى الحساب المرتبط.
- تخزين hash لرمز OTP فقط، مدة صلاحية عشر دقائق، وخمس محاولات كحد أقصى وثلاثة طلبات خلال عشر دقائق.
- تحليل جدول الحفظ عبر Gemini من الخادم بعد التحقق من الجلسة وملكية الحلقة.
- سياسات RLS للعلاقات بين المدير والمعلم والطالب وولي الأمر، وسجل تدقيق لعمليات المدير.
- حماية عروض الرسائل والتسليمات والحلقات من إدخال HTML أو روابط ملفات غير آمنة.
- دورة واجبات كاملة: إنشاء المعلم، تسليم الطالب نصاً أو ملفاً خاصاً، إعادة التسليم قبل التصحيح، التقييم، وتنبيه الطالب.
- مخزن خاص لملفات الواجبات بحد 10 ميجابايت وروابط فتح مؤقتة، ضمن migration `0008_extra_assignments_workflow.sql`.
- اختبارات ثابتة وصياغة آلية لجميع ملفات JavaScript.

## تنفيذ مطلوب على Supabase

1. أنشئ بيئة تجريبية منفصلة، ثم نفذ `supabase db push`.
2. انشر الوظائف `login-with-username` و`admin-create-account` و`account-recovery` و`analyze-schedule`.
3. أضف الأسرار من مدير الأسرار، من دون وضع القيم في Git: `APP_ORIGIN` و`GEMINI_API_KEY` و`RESEND_API_KEY` و`RESEND_FROM_EMAIL`.
4. فعّل Google OAuth، وأضف عنواني العودة الرسميين لصفحة إعدادات الحساب واستعادة كلمة المرور.
5. أنشئ المدير من Supabase Dashboard وفق `ADMIN_BOOTSTRAP.md`.
6. دوّر مفتاح Gemini الذي استُخدم أثناء التطوير وأي جلسات أو كلمات مرور قديمة.

## اختبار قبول قبل الإنتاج

- المدير: إنشاء حساب معلم وطالب وولي أمر، تعديل الدور، إيقاف الحساب، ومراجعة سجل النشاط.
- المعلم: إنشاء حلقة، إضافة طالب، تسجيل التسميع، إنشاء واجب، تصحيح تسليم، وتجربة المساعد الذكي.
- الطالب: الدخول، تغيير كلمة المرور، رؤية الحلقة والورد، تسليم واجب، وحل اختبار دون ظهور مفتاح الإجابة.
- ولي الأمر: ربط الحساب بطفل، رؤية التقدم والتنبيهات، وعدم رؤية طفل غير مرتبط.
- الاستعادة: ربط Google، طلب OTP، إدخال رمز خاطئ عدة مرات، ثم رمز صحيح وتعيين كلمة مرور جديدة.
- العزل: اختبار فتح صفحات ودوال دور آخر بجلسة غير مخولة والتأكد من رفضها.
- التشغيل: اختبار النسخ الاحتياطي والاستعادة، ومراجعة سجلات Edge Functions بعد كل سيناريو.

## فحص محلي

```powershell
& 'C:\Users\alkhy\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --test
```

للمعاينة المحلية فقط استخدم `http://localhost:4173/index.html?preview=1`. لا تستخدم وضع المعاينة مع بيانات حقيقية.
