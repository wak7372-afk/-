# سياسة الأمان التشغيلية

## أسرار يجب تدويرها قبل النشر

- مفتاح Gemini الذي تمت مشاركته أثناء التطوير.
- كلمة مرور أي مدير أُنشئ من ملفات التهيئة القديمة.
- أي جلسات دخول نشأت قبل تنفيذ مراجعة الأمان.

## قواعد إلزامية

- مفاتيح Service Role وGemini والبريد لا توضع في `public/` أو Git.
- الأسرار تحفظ في Supabase Edge Function Secrets فقط.
- مفتاح Supabase العام `anon` ليس سراً، لكن حمايته تعتمد على RLS صحيحة ومختبرة.
- لا تستخدم بيانات طلاب حقيقية في وضع المعاينة المحلية.
- لا يُنشر النظام قبل نجاح اختبارات عزل الأدوار.

## قائمة ما قبل الإنتاج

- [ ] تدوير جميع الأسرار القديمة.
- [ ] إنشاء المدير بالطريقة الموضحة في `ADMIN_BOOTSTRAP.md`.
- [ ] ضبط `APP_ORIGIN` على النطاق الرسمي فقط.
- [ ] تطبيق migrations على بيئة تجريب منفصلة أولاً.
- [ ] نشر Edge Functions واختبارها بحسابات الأدوار الأربعة.
- [ ] تشغيل `npm test` ومعالجة جميع الإخفاقات.
- [ ] اختبار النسخ الاحتياطي والاستعادة.
- [ ] تعطيل المعاينة المحلية عند استخدام بيانات حقيقية.
