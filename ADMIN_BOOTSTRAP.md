# تهيئة حساب المدير الأساسي

لا يُنشأ حساب المدير من migration ولا تُكتب كلمة مروره داخل المشروع.

## إنشاء المدير لأول مرة

1. أنشئ مستخدماً جديداً من Supabase Dashboard > Authentication > Users.
2. استخدم كلمة مرور طويلة وفريدة، وفعّل البريد للمستخدم.
3. انسخ UUID الخاص بالمستخدم.
4. نفّذ الاستعلام التالي في SQL Editor بعد استبدال القيمة فقط:

```sql
update public.users
set
  full_name = 'مدير النظام الرئيسي',
  username = 'warith',
  role = 'admin',
  is_active = true,
  must_change_password = false,
  updated_at = now()
where id = 'REPLACE_WITH_AUTH_USER_UUID'::uuid;
```

5. تأكد أن الاستعلام حدّث صفاً واحداً فقط.
6. سجّل الدخول واختبر الوصول إلى لوحة المدير قبل إنشاء أي حسابات أخرى.

## عند وجود تثبيت سابق

- غيّر كلمة مرور المدير من Supabase Auth فوراً.
- ألغِ جميع الجلسات السابقة من لوحة Supabase.
- لا تعِد استخدام أي كلمة مرور ظهرت في ملف أو محادثة أو سجل أوامر.
- احتفظ بمدير أساسي واحد محمي، وأنشئ المديرين الإضافيين لاحقاً بموافقة صريحة.
