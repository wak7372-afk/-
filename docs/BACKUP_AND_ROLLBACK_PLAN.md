# خطة النسخ والرجوع قبل المرحلة الثانية

## المطلوب قبل أي Migration

1. نسخة مخطط `schema.sql`.
2. نسخة بيانات `data.sql` بصيغة قابلة للاستعادة.
3. حفظ النسختين خارج Git وعدم رفع بيانات المستخدمين إلى مستودع عام.
4. تسجيل وقت النسخ وإصدار Postgres ومشروع المصدر.
5. فحص أن الملفين غير فارغين ويحتوي المخطط على الجداول الأساسية.

## نقطة الرجوع الحالية

- التاريخ: 2026-08-20، بعد Migration `0030` وقبل بوابة القبول الإنتاجي.
- المشروع: `yyqxesnrlgzifydkzkpd`.
- المسار المحلي الخاص: `C:\Users\alkhy\AppData\Local\ZatKhail\Backups`.
- ملف المخطط: `pre-acceptance-0030-schema-20260820-223720.sql`، حجمه `416837` بايت ويحتوي `47` جدولاً.
- ملف البيانات: `pre-acceptance-0030-data-20260820-223720.sql`، حجمه `373921` بايت ويحتوي `47` قسم `COPY`.
- بصمة المخطط: `1D627E3FBBA36B1CDCF5514621324674A99F70ECBC8D68F1CB775AE89332FF89`.
- بصمة البيانات: `3D48AEF7D06FDCAB3C6019562A476CEBC077C92CDE86BB1FABE6C58E8FDDC4F3`.
- نتيجة الاستعادة: تطابق `47/47` جدولاً و`893/893` صفاً، بلا فروق.
- طريقة الاختبار: استعادة المخطط والبيانات في قاعدة Supabase محلية معزولة، مع تعطيل المشغلات أثناء إدخال البيانات لمعالجة العلاقات الدائرية.

## نقطة رجوع تاريخية قبل 0027

- التاريخ: 2026-08-20، قبل Migration `0027_direct_messaging_hardening.sql`.
- المشروع: `yyqxesnrlgzifydkzkpd`.
- المسار المحلي الخاص: `C:\Users\alkhy\AppData\Local\ZatKhail\Backups`.
- الملفات: `pre-0027-schema-20260820-183920.sql` و`pre-0027-data-20260820-183920.sql`.
- التحقق: ملف المخطط `360531` بايت ويحتوي `45` جدولاً، وملف البيانات `396712` بايت ويحتوي `74` قسم `COPY`.
- بصمة المخطط: `AA5A9F269E9869838D57D1F882536B870C23C2A805245254B469D4A2431788E5`.
- بصمة البيانات: `83CC07212C56E0B8957C0BED753304112BFCE65F3706B985F013D7CC8AE60DA3`.
- تنبيه الاستعادة: توجد علاقات دائرية في `users` و`quran_reports` و`quran_report_assignments`؛ يستعاد المخطط أولاً ثم البيانات مع تعطيل المشغلات مؤقتاً.

## الخيار المحلي الموصى به

تثبيت Docker Desktop ثم استخدام Supabase CLI المرتبط بالمشروع لإنشاء نسخ المخطط والبيانات. تحفظ الملفات مؤقتا في `scratch/backups` المستبعد من Git، ثم تنقل إلى موقع خاص وآمن.

## الخيار المستمر للإنتاج

استخدام خطة Supabase توفر نسخا يومية، أو إعداد نسخة دورية مشفرة في مخزن خاص. النسخة الدورية لا تلغي ضرورة اختبار الاستعادة.

## قاعدة الرجوع لكل Migration

- تكون Migration إضافية وتوافقية ولا تحذف الجداول القديمة في الدفعة الأولى.
- يوثق لكل تغيير SQL رجوع معاكس أو طريقة تعطيل الميزة.
- عند فشل التطبيق يوقف النشر، ولا تعاد المحاولة قبل تحليل السجل.
- لا تستعاد قاعدة الإنتاج إلا بقرار صريح وبعد تحديد نقطة الاستعادة وفترة التوقف.

## تطبيق 0027

- طُبقت Migration `0027_direct_messaging_hardening.sql` في 2026-08-20 بعد نجاح النسخة أعلاه.
- تحقق `supabase migration list --linked` من تطابق الإصدار محلياً وبعيداً.
- نجح `supabase db lint --linked --level warning` بلا أخطاء.
